<!DOCTYPE html>
<html>
  <head>
    <title>SportmanGlobalBackend</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>
    // src/components/HomePage.js

import React from 'react';

const HomePage = () => {
  return (
    <div>
      <header>
        <h1>Your Soccer Live Score Website</h1>
      </header>
      <section>
        <h2>Live Matches</h2>
        {/* Display live matches with team names, scores, and time remaining */}
        {/* Add filters or sorting options */}
      </section>
      <section>
        <h2>Upcoming Matches</h2>
        {/* Display upcoming matches with date, time, and participating teams */}
      </section>
      <section>
        <h2>Recent Results</h2>
        {/* Display recent match results */}
      </section>
      <section>
        <h2>League Standings</

  </body>
</html>
